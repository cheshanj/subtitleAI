# Normalize line endings: LF in the repo, native in working copies.
* text=auto eol=lf

# Windows-only scripts keep CRLF.
*.bat text eol=crlf

# Treat notebooks and data as-is.
*.ipynb text eol=lf
*.tsv binary
